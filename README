# Blog
this is we try to create blog with nextjs


## create project with `nextjs`

```
>npm init -y && npm install react react-dom next
```

## create pages

```
>mkdir pages && touch index.js

```

> `index.js`

```
const landing = () => {
	return <h1>landing page<h1>;
}

export default landing;
```
